<% @photos.each do |photo| %>

  <div class='row'>

    <div class='col-sm-6'>
    <%= link_to image_tag(photo.image.url, :class => 'photo mx-auto'), photo.image.url, target: '_blank' %>
    </div>
    <div class='col-sm-6'>
      <%= photo.caption %>
    </div>
    
  </div>
<% end %>
